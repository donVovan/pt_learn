// ⋕js.Pm.SM.St.5
'use strict';
let str = 'abcde';
if (str.indexOf('a') >= 0) {
    document.write('Элемент находится на позиции:' + str.indexOf('a'));
} else {
    document.write('Нет такого элемента!');
}
